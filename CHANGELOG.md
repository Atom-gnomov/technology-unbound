# Changelog — Unbound Technology

Версия задаётся в `build.gradle`. Схема фаз: репо порта,
`docs/integration/ic2_v5_impl_skeleton.md` §7 (0.1 → 1.0).

## [0.2.0] — Фаза 3а: конвертеры энергии (ждёт первой локальной сборки)

- **Таум-Генератор** (`unboundtech:thaum_generator`): тянет Ignis/Potentia из
  узлов в 8 блоках, отдаёт EU как источник LV (32 EU/t, буфер 20k).
  Пол 20% по-аспектной ёмкости — узлы не убиваются. Два генератора ближе
  16 блоков глушат друг друга (реестр позиций вместо сканирования мира).
- **Эфирный Двигатель** (`unboundtech:aetheric_engine`): приёмник MV
  (буфер 40k), пополняет недостающие аспекты узлов по 8,000 EU за единицу.
  Ёмкость узла (aspectsBase) не трогает.
- **Слиток Таум-Стали** + рецепт тигля из `ingot/steel` IC2, оредикт
  `ingotThaumSteel`.
- 4 новых исследования (THAUM_STEEL, VIS_TO_EU_GENERATOR, EU_TO_VIS_ENGINE,
  LORE_RESONANCE_LIMITS) со страницами рецептов; арканные рецепты обеих машин.
- Инфраструктура: базовый класс машины (FACING+ACTIVE), общая база тайлов
  с кэшем узлов (паттерн Пьедестала жезла из порта), реестр генераторов,
  креатив-вкладка, клиентская регистрация моделей.
- Ассеты: блокстейты/модели/текстуры машин (вкл. светящуюся «морду»),
  текстура слитка, локализация ru/en.
- Ключевой факт, определивший дизайн: **ауры чанков в порте не существует**
  (TC6-фасад мёртв) — двигатель заряжает узлы, а не абстрактную ауру.
- ПКМ пустой рукой по машине — строка статуса (буфер / интерференция).
- Аспекты для собственных предметов мода (таум-сталь, обе машины).

Найдено и исправлено адверсариальной проверкой до первой сборки (20 находок):
`EnumFacing.getHorizontal` не существует в stable_39 (нужен
`byHorizontalIndex`); ключи блокстейтов обязаны идти в алфавитном порядке
свойств (`active=…,facing=…`), иначе все состояния рендерятся «пропавшей
моделью»; модовому тайлу нужен `shouldRefresh`, иначе смена ACTIVE
пересоздаёт тайл и обнуляет буфер EU по несколько раз в секунду; ACTIVE
получил гистерезис (иначе мигал каждые 20–40 тиков); `nodeAt` мог синхронно
подгружать выгруженные чанки; пересканирование узлов запускалось даже когда
работать было нечем; пол 20% мог обнулить аспект с нулевой ёмкостью;
`markDirty` вызывался каждый тик из энергетических колбэков.

## [0.1.0] — Фаза 1: каркас (не собрано, ждёт первой локальной сборки)

- Каркас мода: modid `unboundtech`, зависимости
  `required-after:thaumcraft, ic2`; `after:mets, advanced_solar_panels`.
- Модульная система (`UTModule`/`ModuleManager`): двойной гейт
  конфиг × наличие мода-компаньона; SRP-модуля нет намеренно (v5 §7).
- `EnergyCanon`: курсы v5 (2,000 / 8,000 / 20,000 EU), переопределяются
  конфигом, WARN при нарушении «второго закона».
- Аспекты ~27 предметов IC2 (`IC2Aspects`), безопасная регистрация с WARN
  на отсутствующие предметы.
- Вкладка Таумономикона «Индустриальная Магия»: скрытый корень
  `THAUM_IC2_INTRO` с item/aspect-триггерами (открывается сканированием
  машины IC2), 2 лорных исследования, `THAUM_ORE_MACERATION`,
  `ALUMENTUM_FUEL`; ланг en/ru, иконка вкладки.
- Рецепты: дробитель (киноварь→2 ртути, янтарь→2, серебро→2 пыли IC2,
  6 зачарованных руд→2 осколка, кластер→4 осколка), компрессор (6 осколков→
  кластер — курс крафта порта, 4 янтаря→блок), экстрактор
  (таинт-слизь/тендрил → резина, серебролист → 2 живичные смолы).
- Алюментум-топливо — без кода: порт TC4U 1.2.8.0 вернул burn time 6400.
- `docs/IC2_API_ASSUMPTIONS.md`: все допущения по IC2 API для первой сборки.
