# Changelog — Unbound Technology

Версия задаётся в `build.gradle`. Схема фаз: репо порта,
`docs/integration/ic2_v5_impl_skeleton.md` §7 (0.1 → 1.0).

## [0.2.2] — Правки по сквозной проверке канона против кода

Найдено сверкой кода с исходниками порта и с jar-ом IC2 2.8.195
(`docs/design/04_systems/T13_crosscheck.md` в репо порта).

- **Убран наш рецепт дробления серебряной руды.** IC2 обрабатывает её сам:
  в `assets/ic2/config/macerator.ini` лежит
  `OreDict:oreSilver = ic2:crushed#silver*2`, и у него есть вся цепочка —
  crushed → purified → dust → ingot и термоцентрифуга
  (`crushedPurifiedSilver → small_lead + silver @heat:1000`). Наш рецепт
  `oreSilver → 2× dust` шёл с `overwrite = false`: он либо молча не вставал,
  либо уводил руду мимо промывателя, оставляя серебро единственной рудой
  пака без обогащения. Киноварь, янтарь, зачарованные руды и кластеры —
  по-прежнему наши, в конфигах IC2 их нет.
- **`OverclockRules` переписан под принятую механику.** Удалены
  `CHUNK_LIMIT` и `PER_MACHINE_LIMIT` вместе с их конфиг-ключами: ускорение
  в IC2 применяется только предметами в слотах апгрейдов
  (`InvSlotUpgrade` → `IProcessingUpgrade`), число слотов задаёт сама IC2
  (`new InvSlotUpgrade(this, "upgrade", 4)`), а учёт «по чанку» отменён в
  пользу перегрева внутри самого апгрейда. Вместо них — множители
  (0.6 времени / 1.2 потребления против 0.7 / 1.6 у родного оверклокера),
  заряд эссенции и параметры перегрева.
- **`EnergyCanon` переведён с «ауры чанка» на узлы.** `EU_PER_AURA_SELL` /
  `EU_PER_AURA_BUY` → `EU_PER_NODE_ASPECT_SELL` / `EU_PER_NODE_ASPECT_BUY`,
  javadoc и предупреждения `validateSecondLaw` переписаны, ссылка ведёт в
  канон, а не в архив. Ауры чанков в порте не существует
  (`AuraHandler` — мёртвый фасад), и имя константы приглашало написать
  следующий конвертер «в ауру». Числа не изменились: 2 000 / 8 000 / 20 000.
- **Конфиг:** ключи `energy.eu_per_aura_unit_generated` и
  `energy.eu_per_aura_unit_restored` переименованы в
  `energy.eu_per_node_aspect_generated` / `..._restored`; секция `overclock`
  заменена целиком. Старые ключи в существующих `unboundtech.cfg`
  игнорируются — значения по умолчанию те же.
- **Аспекты наших объектов регистрируются явно, по формуле** (решение
  владельца по П-6). Новый пакет `unboundtech.aspects`:
  `AspectFormula` считает `нормализация(порог(0.6 × Σ аспектов компонентов)
  + подпись процесса)` — компоненты берутся прямо из зарегистрированного
  арканного рецепта, их аспекты читаются у ТК в рантайме
  (`ThaumcraftApiHelper.getObjectAspects`), размер стека учитывается;
  `UTAspects` регистрирует итог через `ThaumcraftApi.registerObjectTag`.
  Без этого аспекты выводил бы сам ТК (`generateTags` по рецептам), но по
  своим правилам — и числа в карточках канона были бы фикцией.
  Вся цепочка вычисления печатается в лог одной строкой. Коэффициент
  передачи настраивается (`aspects.transfer_coefficient`) и зажимается в
  канонические 0.5…0.75.
  Посчитанные итоги: Таум-Генератор — `Metallum 10, Praecantatio 6,
  Machina 6, Ignis 4, Potentia 2`; Эфирный Двигатель — `Metallum 11,
  Machina 7, Praecantatio 5, Cognitio 2`.
- **Дробилка: ×3 осколка с чистой руды ТК** (было ×2) — решение владельца:
  руды роняют осколки, блок достаётся только Шёлковым касанием, и ×2
  проигрывали кирке с Фортуной III. Полная схема с кристальной пылью и
  синтетическими кристаллами — `ore_processing.md` в репо порта; новые
  предметы будут в следующей фазе.
- **Аспекты трёх солнечных шлемов ASP регистрировались в никуда.** Мы
  использовали snake_case (`advanced_solar_helmet`), а ASP регистрирует
  предметы в lowerCamelCase: `SolarHelmetTypes.getName()` возвращает
  `this.name + "SolarHelmet"`, то есть `advancedSolarHelmet`,
  `hybridSolarHelmet`, `ultimateSolarHelmet`. Snake_case у ASP встречается
  только в путях моделей — на этом и легко обмануться. Шлемы оставались
  без аспектов: не сканировались и ничего не давали в тигле.
- **Версия синхронизирована:** `UnboundTech.VERSION` отставал от
  `build.gradle` (0.2.0 против 0.2.1), из-за чего `@Mod` объявлял одну
  версию, а mcmod.info — другую. Обе — 0.2.2.

## [0.2.1] — Фиксы фазы 3а по результатам адверсариальной проверки

Четыре дефекта, найденные независимой проверкой кода против исходников
порта и Forge (сборка их не ловит — все проявляются только в игре):

- **Блокстейты не резолвились ни в одном состоянии.** Ключи вариантов были
  записаны в порядке объявления свойств (`facing=…,active=…`), а 1.12.2
  строит строку по `ImmutableSortedMap` — то есть в АЛФАВИТНОМ порядке
  (`active=…,facing=…`). Все 8 состояний обеих машин рендерились бы
  «пропавшей моделью»; предмет в руке при этом выглядел нормально
  (вариант `inventory` резолвится отдельно), что и маскировало баг.
  Доказательство: все 328 многосвойственных ключей в блокстейтах порта
  отсортированы по алфавиту, включая случаи, где это расходится с порядком
  объявления (`blockmagnet.json`: `mob=…,pulling=…` при `(PULLING, MOB)`).
- **Смена ACTIVE пересоздавала тайл** (обнуление буфера EU + вылет из
  энергосети IC2 по нескольку раз в секунду). Комментарий в
  `BlockMachineBase.setActive` был верен только для ВАНИЛЬНЫХ тайлов:
  Forge возвращает `!isVanilla || блок изменился`, а `isVanilla`
  определяется по имени пакета. Добавлен `shouldRefresh` в оба тайла —
  ровно как в восьми тайлах самого порта.
- **`NodeCache.nodeAt` мог синхронно подгружать выгруженные чанки** из тика
  машины: позиции живут в кэше до 100 тиков, а `getTileEntity` на выгруженный
  чанк вызывает загрузку/генерацию. Добавлен `isBlockLoaded`.
- **Пол узла мог обнулить аспект**: при `getNodeVisBase(aspect) == 0`
  (возможно у сторонних реализаций `INode`) 20%-пол вырождался в ноль и
  аспект удалялся из узла навсегда — реген обходит только `aspectsBase`.
  Теперь пол не ниже 1.

## [0.2.0] — Фаза 3а: конвертеры энергии

Первые блоки мода. Мост между висом и EU построен в обе стороны, и он
намеренно убыточен: цикл вис → EU → вис возвращает 25%, вечного двигателя
не существует.

- **Таум-Генератор** (`unboundtech:thaum_generator`) — вис узла → EU.
  Буфер 20,000 EU, tier 1 (LV). Раз в 20 тиков тянет 1 Ignis/Potentia из
  ближайшего узла (куб ±8) и даёт 2,000 EU. Узел не выдаивается: работает
  пол в **20% по-аспектной ёмкости** (`getNodeVisBase`), ниже машина не
  берёт. Долговременный потолок задаёт реген самого узла, а не машина.
  Интерференция: два генератора ближе 16 блоков глушат друг друга.
- **Эфирный Двигатель** (`unboundtech:aetheric_engine`) — EU → вис узла.
  Буфер 40,000 EU, tier 2 (MV). Раз в 20 тиков доливает 1 недостающий
  аспект за 8,000 EU (вчетверо дороже, чем платит генератор). Ёмкость узла
  (`aspectsBase`) не трогается — лечение угасших узлов это отдельная тема
  поздних фаз.
- Инфраструктура блоков: `BlockMachineBase` (FACING+ACTIVE, мета
  `facing | active<<3`), регистрация блоков/предметов/тайлов, вкладка
  креатива, клиентская регистрация моделей.
- `NodeCache`: общий кэш узлов ±8 с пересканированием не чаще раза в 100
  тиков и только после неудачи; банки-узлы исключены; синк узла после
  каждой мутации (иначе таумометр показывает застывшие цифры).
- Исследования `VIS_TO_EU_GENERATOR`, `EU_TO_VIS_ENGINE`,
  `LORE_RESONANCE_LIMITS` + арканные рецепты (страницы держат сами объекты
  рецептов). Корпус — таумий; таум-сталь придёт в фазе 3в (TODO в коде).
- Ассеты: текстуры машин (первая итерация, арт-гайд §2.5 — корпус серый
  с пурпурным отливом, «морда» IC2-паттерном), блокстейты на 4 поворота
  × 2 состояния, модели, ланг en/ru.
- **Первые тесты сабмода**: `ConverterCanonStaticGuardTest` пинит курсы
  v5, пол узла, тиры/буферы и то, что смена ACTIVE не переустанавливает
  тайл (иначе машина тикала бы дважды за тик — поймано при реализации).
- Сверено с реальным jar-ом: `BasicSource`/`BasicSink(TileEntity, double,
  int)` и весь их жизненный цикл — как в §5 спеки.

## [0.1.2] — Нитор-тепло: фаза 2 закрыта

- Исследование `NITOR_HEAT` (ключ из rt_1_ic2.md): нитор у Стирлинга /
  жидкостного теплообменника IC2 — вечные 20 HU/t (~10 EU/t). Само
  поведение живёт в порте ≥1.2.8.1 (`TileNitor implements IHeatSource`
  за @Optional — потребители тепла IC2 ищут источник жёстким instanceof,
  снаружи интерфейс не навесить; хук-таблица phase1_core_spec §7 уточнена).
- Сабмод собран против dev-jar порта 1.2.8.1.
- Ланг en/ru. Ветка исследования продолжится нитор-термальным генератором
  в фазе 3+.
- Этим закрыт последний пункт фазы 2 скелета (§7): дробитель/компрессор/
  экстрактор ✓, алюментум-топливо ✓ (1.2.8.0), нитор-тепло ✓ (1.2.8.1),
  аспекты ✓. До релиза 0.1 — игровые приёмки phase1_core_spec §8.

## [0.1.1] — Компат-модули METS и ASP (внеплановый срез фазы 10)

- modid аддонов сверены с реальными jar-ами (было допущение из сети):
  MoreElectricTools.v1.662 → `mets`, Advanced Solar Panels-4.3.0 →
  `advanced_solar_panels` — константы `CompatIds` совпали, TODO сняты.
- `ModItems`: безопасный доступ к предметам аддонов через реестр Forge
  (WARN + пустой стек на отсутствующий предмет — контракт IC2Handles).
- Модуль `asp` (при наличии ASP): аспекты 23 позиций — 6 машин
  (`advanced_solar_panels:machines`, меты TEs 0–5, сверено декомпиляцией),
  14 материалов (`:crafting`, меты CraftingTypes 0–13), 3 солнечных шлема.
  Саннариум получил Lux 8/Potentia 4/Ignis 2 → тигель плавит его в эссенцию
  света и силы («Sunnarium в тигле», ic2_v4_submod.md §4). Исследование
  `SOLAR_SUNNARIUM` во вкладке (скрыто без ASP).
- Модуль `mets` (при наличии METS): аспекты 26 куриров. позиций — руды,
  титан/ниобий/торий, сверхпроводники, «супер»-тиры, живой металл (Victus),
  оружие, геомагнитный генератор. Исследование `TECHNO_MATERIALS`.
- Ланг en/ru: 2 исследования, 4 страницы лора.
- Правило v4 submod §4 соблюдено: контент аддонов не дублируется, жёстких
  зависимостей нет; альт-рецепты (Тесла-фокус, компоненты жезлов через
  саннариум) отложены до фаз 3+, в лоре — намёки.
- Jar-ы аддонов в `run/mods/` для dev-запуска; корневые `*.jar` в .gitignore.

## [0.1.0] — Фаза 1: каркас (собран 2026-08-06, IC2 API подтверждён)

- Каркас мода: modid `unboundtech`, зависимости
  `required-after:thaumcraft, ic2`; `after:mets, advanced_solar_panels`.
- Модульная система (`UTModule`/`ModuleManager`): двойной гейт
  конфиг × наличие мода-компаньона; SRP-модуля нет намеренно (v5 §7).
- `EnergyCanon`: курсы v5 (2,000 / 8,000 / 20,000 EU), переопределяются
  конфигом, WARN при нарушении «второго закона».
- Аспекты ~27 предметов IC2 (`IC2Aspects`), безопасная регистрация с WARN
  на отсутствующие предметы.
- Вкладка Таумономикона «Индустриальная Магия»: скрытый корень
  `THAUM_IC2_INTRO` с item/aspect-триггерами (открывается сканированием
  машины IC2), 2 лорных исследования, `THAUM_ORE_MACERATION`,
  `ALUMENTUM_FUEL`; ланг en/ru, иконка вкладки.
- Рецепты: дробитель (киноварь→2 ртути, янтарь→2, серебро→2 пыли IC2,
  6 зачарованных руд→2 осколка, кластер→4 осколка), компрессор (6 осколков→
  кластер — курс крафта порта, 4 янтаря→блок), экстрактор
  (таинт-слизь/тендрил → резина, серебролист → 2 живичные смолы).
- Алюментум-топливо — без кода: порт TC4U 1.2.8.0 вернул burn time 6400.
- `docs/IC2_API_ASSUMPTIONS.md`: все допущения по IC2 API для первой сборки.
- Первая сборка (2026-08-06): компиляция с первого раза против IC2
  2.8.222-ex112 (файл 3838713) + dev-jar порта 1.2.8.0 (ветка
  `claude/ic2-thaumcraft-integration-agwnn4`, в main ещё не влита).
  Все допущения IC2 API подтверждены: сигнатуры — компилятором, пары
  name/variant — по enum-ам jar-а, `forStack` = размер стека —
  декомпиляцией. Детали и статусы — в `docs/IC2_API_ASSUMPTIONS.md`.
  Впереди до релиза 0.1: игровые приёмочные пункты `phase1_core_spec.md` §8
  (скан машины открывает вкладку, рецепты, ~16k EU с алюментума).
